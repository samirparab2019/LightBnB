const isOdd = function(num) {
  return num % 2 === 0;
}
console.log("3 is odd: " + isOdd(3));
console.log("8 is odd: " + isOdd(8));


//this returns true for even numbers and false for odd numebrs.
//also below is same as above.
